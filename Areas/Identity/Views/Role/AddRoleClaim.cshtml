@model EditClaimModel
@{
    ViewData["Title"] = "Gán đặc tính cho role";
}
<div class="mb-4 d-flex align-items-center">
    <a asp-controller="Home" asp-action="AdminIndex" class="text-dark"><i class="fa-solid fa-house px-2"></i>Trang chủ</a>
    <i class="fa-solid fa-chevron-left px-2"></i>
    <a asp-action="Index" class="text-dark"> Danh sách role</a>
</div>
<h2>@ViewData["Title"]: @Model.role.Name</h2>
<partial name="_StatusMessage"></partial>

<hr>

<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="@Model.ClaimType" class="control-label"></label>
                <input asp-for="@Model.ClaimType" class="form-control" />
                <span asp-validation-for="@Model.ClaimType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.ClaimValue" class="control-label"></label>
                <input asp-for="@Model.ClaimValue" class="form-control" />
                <span asp-validation-for="@Model.ClaimValue" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Tạo mới" class="btn btn-primary" />
                <a class="btn btn-primary" asp-action="Edit" asp-route-roleid="@Model.role.Id">Edit role</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
